#include <bits/stdc++.h>
using namespace std;

int main(){
	int q;
	cin >> q;
	while(q-- > 0){
		char b,p,f,h,c;
		cin >> b >> p >> f >> h >> c;
		b/=2;
		int choice = max(h,c) * b;
	}
	
	
	return 0;
}
